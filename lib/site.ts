// Shared site constants (iter-32).
//
// The permanent PUBLIC domain used for citations and any canonical absolute URL
// the app hands to a reader. This is deliberately NOT the vault7a deploy host:
// a citation must point at the durable public address, not the box it runs on.
export const CANONICAL_URL = "https://mapsofbharat.in";
